<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="huellaProto.Views.ListFlota"
             BindingContext="{Binding Main,Source={StaticResource Locator}}"
             Title="Configuraciones">
    <ContentPage.Content>
        <StackLayout BindingContext="{Binding FlotaC}">
            <Label
                Text="Total vehiculos 1000" FontAttributes="Bold" FontSize="Large" HorizontalTextAlignment="Center" Margin="0,15,0,0"></Label>
            <!--<ListView 
                
                HasUnevenRows="true" 
                IsPullToRefreshEnabled="true"
              
                ItemsSource="{Binding Prueba}"
                SeparatorVisibility="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Frame
                                CornerRadius="20"
                                HasShadow="true"
                                Margin="5"
                                BackgroundColor="#CEF6F5">
                                --><!--<Frame.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding SelectLandCommand}"/>
                                </Frame.GestureRecognizers>--><!--
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="auto"/>
                                        --><!--<ColumnDefinition Width="Auto"/>--><!--
                                    </Grid.ColumnDefinitions>
                                    --><!--<ffimageloading:CachedImage 
                                        Grid.Column="0"
                                        HeightRequest="30"
                                        Source="{Binding Flag, Converter={StaticResource SvgImageSourceConverter}}"
                                        WidthRequest="60">
                                    </ffimageloading:CachedImage>--><!--
                                    <StackLayout 
                                        Grid.Column="0"
                                        VerticalOptions="Center">
                                        <Label 
                                            FontAttributes="Bold"
                                            FontSize="Medium" 
                                            Text="{Binding Motor}"
                                            TextColor="White">
                                        </Label>
                                        <Label 
                                            Text="{Binding Combustible}"
                                            TextColor="Black">
                                        </Label>
                                    </StackLayout>
                                    --><!--<Image 
                                        Grid.Column="1"
                                        HorizontalOptions="End" 
                                        Source="ic_chevron_right">
                                    </Image>--><!--
                                </Grid>
                            </Frame>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->
            <ListView x:Name="MenuItemsListView"
                        SeparatorVisibility="None"
                        HasUnevenRows="true"
                        ItemsSource="{Binding Prueba}"
                        VerticalOptions="Start"
                        Margin="0,15,0,0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="15,10" HorizontalOptions="FillAndExpand"
                                            Orientation="Horizontal">
                                <Image Source="truck"></Image>
                                <Label VerticalOptions="FillAndExpand"
                                    VerticalTextAlignment="Center"
                                    Text="{Binding Motor}"
                                    FontSize="24">
                                    <Label.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding NavegarCommand}"/>
                                    </Label.GestureRecognizers>
                                </Label>
                                <Image 
                                    Grid.Column="1"
                                    HorizontalOptions="EndAndExpand" 
                                    Source="ic_chevron_right">
                                </Image>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>